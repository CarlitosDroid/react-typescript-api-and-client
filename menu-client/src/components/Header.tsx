import React from "react";

const Header: React.FC = () => {
    return (
        <div>
            <label className="menu-header">BIENVENIDO A LA TIENDA DON PURO</label>
        </div>
    );
};

export default Header;
